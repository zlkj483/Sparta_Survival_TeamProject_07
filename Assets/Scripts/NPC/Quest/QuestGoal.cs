using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/*public enum QuestGoalType { Gather }
[System.Serializable]
public class QuestGoal // 퀘스트의 개별 목표
{
    // ... (기존 필드 그대로 유지) ...
    public QuestGoalType goalType; // 목표 타입
    public string targetID; // 목표 대상
    public int requiredAmount; // 목표치
    public int currentAmount; // 현재진행도
    public bool IsComplete => currentAmount >= requiredAmount; // 달성여부

    // 퀘스트 목표 복사 생성자 추가 (깊은 복사를 위해 필요)
    public QuestGoal(QuestGoal template)
    {
        this.goalType = template.goalType;
        this.targetID = template.targetID;
        this.requiredAmount = template.requiredAmount;

        // 런타임 데이터인 currentAmount는 새 퀘스트이므로 0으로 초기화
        this.currentAmount = 0;
    }

    // 기본 생성자를 추가합니다.
    public QuestGoal() { }
}*/